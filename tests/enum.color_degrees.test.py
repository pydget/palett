from pyspare import deco_vector

from palett.enum.color_degrees import accents, lightens, darkens, readable

print(deco_vector(accents))
print(deco_vector(lightens))
print(deco_vector(darkens))
print(deco_vector(readable))
